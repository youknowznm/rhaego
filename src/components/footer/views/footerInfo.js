import React from 'react'
import PropTypes from 'prop-types'

const styles = (theme) => ({
  root: {
    width: '100%',
    background: '#fafafa',
    height: 72,
    lineHeight: 72,

  },

})

const footerInfo = ({classes}) => (
  
)
